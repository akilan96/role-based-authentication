version: "3"

services:
  backend:
    build: ./backend
    ports:
      - "4500:4500"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend/client
    ports:
      - "5173:5173"
    depends_on:
      - backend