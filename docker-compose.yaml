version: "3"

services:
  backend:
    build: ./backend
    ports:
      - "4500:4500"
    environment:
      - MONGO_URI=environment=mongodb+srv://ourpremium0_db_user:Akilan123@cluster0.adzhtgu.mongodb.net/roleauth?retryWrites=true&w=majority
      - PORT=4500
      - SECRET=AKI
      - REFRESH=KABI

  frontend:
    build: ./frontend/client
    ports:
      - "5173:5173"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://16.170.249.100:4500
   